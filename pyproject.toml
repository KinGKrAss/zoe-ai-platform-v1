[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "zoe-ai-platform-v1"
version = "1.0.0"
description = "Zoë AI Platform for the Z1 Real Estate Command Center"
requires-python = ">=3.11"
readme = "README.md"
license = {text = "Proprietary"}
authors = [{name = "KinGKrAss"}]
dependencies = []

[project.optional-dependencies]
test = [
  "pytest>=8.3,<9",
  "pytest-cov>=5,<7",
]
dev = [
  "ruff>=0.12,<1",
  "mypy>=1.16,<2",
]

[tool.setuptools]
include-package-data = true

[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["services"]
python_files = ["test_*.py", "*_test.py"]
addopts = "-ra --strict-config"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.coverage.run]
branch = true
source = ["services"]

[tool.coverage.report]
show_missing = true
skip_covered = true
